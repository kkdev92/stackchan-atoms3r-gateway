bin/
obj/
.packages/
TestResults/

# Packages produced by pack-sdk.ps1. nuget.config points a package source here so the
# reference host restores the SDK as packages; the directory is created on demand.
artifacts/

# Local configuration for AI-assisted development sessions. Machine-specific.
.claude/

# Local MCP server configuration for AI-assisted development. Machine-specific.
.mcp.json

# Local authentication tokens and API keys.
.secrets/
.env
*.env
!.env.example

# Audio generated by device-sim or diagnostics may contain conversation content.
*.wav
*.mp3
*.pcm

# Workspace for plans and investigation notes that are not published artifacts.
work/
